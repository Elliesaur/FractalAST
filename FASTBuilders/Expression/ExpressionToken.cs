namespace FractalAST.FASTBuilders.Expression
{
    public class ExpressionToken : BaseToken
    {
        public ExpressionToken(ExprType type, object value)
            : base(type, value)
        {
            
        }
    }
}
